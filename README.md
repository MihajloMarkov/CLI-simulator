# CLI-simulator
A custom command-line interpreter (shell) implementation in C++ that mimics core functionalities of Unix/Linux shells. Supports built-in commands (echo, wc, tr, head, touch, rm, etc.), I/O redirection, piping, batch processing, and error handling with comprehensive object-oriented design.
